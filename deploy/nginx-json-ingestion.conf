# ============================================================
#  Nginx 反向代理 + HTTPS 配置示例
#  用法: 复制到 /etc/nginx/conf.d/json-ingestion.conf 后 nginx -t && systemctl reload nginx
#  证书: certbot 免费证书或企业证书, 替换下面的 cert.pem / key.pem
#  前提: 应用仍只监听 127.0.0.1:3100, 由 Nginx 对外提供 HTTPS
# ============================================================

# 全局收敛: 不暴露版本号
server_tokens off;

# 后端只信任来自本机反代的 X-Forwarded-For, 因此这里必须用 $remote_addr 覆盖,
# 而不是 $proxy_add_x_forwarded_for(后者会把客户端自带的伪造值一起带上)。
server {
    listen 443 ssl;
    http2 on;
    server_name ingestion.example.com;   # 改成你的域名

    ssl_certificate     /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 1d;

    client_max_body_size 20m;

    # 安全响应头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "no-referrer" always;
    add_header Content-Security-Policy "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; script-src 'self'; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;

    location / {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # CSV 导出等耗时接口单独放宽超时
    location /api/export/ {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # 手动执行任务是同步接口, 大接口可能超过默认 120s
    location ~ ^/api/tasks/[0-9]+/(run|full-refresh)$ {
        proxy_pass http://127.0.0.1:3100;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $remote_addr;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 900s;
        proxy_send_timeout 900s;
    }
}

# HTTP -> HTTPS 跳转
server {
    listen 80;
    server_name ingestion.example.com;   # 改成你的域名
    return 301 https://$host$request_uri;
}
