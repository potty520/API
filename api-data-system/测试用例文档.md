# API 数据解析与自动入库系统 — 测试用例文档

> 生成日期：2026-05-20 | 测试总数：102 | 全部通过

---

## 一、后端服务层测试（52 个用例）

### 1.1 CronExpressionParserTest（18 个用例）

| #    | 用例名称                                             | 输入                                                                                          | 预期结果                 |
| ---- | ------------------------------------------------ | ------------------------------------------------------------------------------------------- | -------------------- |
| 1    | should return now + 5 minutes for next execution | from=2026-05-20 10:00                                                                       | 返回 2026-05-20 10:05  |
| 2–8  | should validate correct 5-field cron expressions | `0 */5 * * *`、`0 0 * * *`、`0 0 8 * *`、`0 9 * * 1`、`*/1 * * * *`、`1,2,3 * * * *`、`0 0 0 1 1` | `isValid()` 返回 true  |
| 9–14 | should reject invalid cron expressions           | null、空字符串、空格、`invalid`、`0 */5 * * * *` (6字段)、`* * * *` (4字段)、`a b c d e`                    | `isValid()` 返回 false |
| 15   | should describe a valid cron expression          | `0 */5 * * *`                                                                               | 描述包含 "每"、"分"、"时"     |
| 16   | should return error description for invalid cron | `"bad"`                                                                                     | "无效的 Cron 表达式"       |
| 17   | should return error description for null         | null                                                                                        | "无效的 Cron 表达式"       |

### 1.2 ApiConfigServiceTest（7 个用例）

| #   | 用例名称                                                          | 输入                                        | 预期结果                             |
| --- | ------------------------------------------------------------- | ----------------------------------------- | -------------------------------- |
| 1   | should find configs by page with keyword filter               | pageNum=1, pageSize=10, keyword="weather" | 返回 Page 对象，验证 selectPage 被调用     |
| 2   | should find configs by page without keyword                   | pageNum=1, pageSize=10, keyword=null      | 返回 Page 对象                       |
| 3   | should find config by id                                      | id=1                                      | 返回 name="Test API"               |
| 4   | should save new config with create time                       | ApiConfig(name="New API", id=null)        | createTime 和 updateTime 被设置      |
| 5   | should update existing config without overwriting create time | ApiConfig(id=1, name="Updated API")       | createTime 为 null，updateTime 被设置 |
| 6   | should delete config by id                                    | id=1                                      | 返回 true                          |
| 7   | should find configs by token config id                        | tokenConfigId=1                           | 返回包含 1 条记录的列表                    |

### 1.3 TokenConfigServiceTest（7 个用例）

| #   | 用例名称                                                           | 输入                                          | 预期结果                              |
| --- | -------------------------------------------------------------- | ------------------------------------------- | --------------------------------- |
| 1   | should find all token configs                                  | 无                                           | 返回包含 "Test Token" 的列表             |
| 2   | should save new token with create time                         | TokenConfig(name="New Token", type="FIXED") | createTime 和 updateTime 被设置       |
| 3   | should update existing token without overwriting create time   | TokenConfig(id=1, name="Updated")           | createTime 为 null，updateTime 被设置  |
| 4   | should delete token by id                                      | id=1                                        | 返回 true                           |
| 5   | should find active tokens                                      | 无                                           | 返回状态为 ACTIVE 的 token              |
| 6   | should update token status successfully                        | id=1, status="INACTIVE"                     | status 变为 INACTIVE，updateTime 被设置 |
| 7   | should return false when updating status of non-existent token | id=999                                      | 返回 false                          |

### 1.4 ScheduledTaskServiceTest（7 个用例）

| #   | 用例名称                                                | 输入                                           | 预期结果                                             |
| --- | --------------------------------------------------- | -------------------------------------------- | ------------------------------------------------ |
| 1   | should save new task with default counts            | ScheduledTask(name="Test", code="TEST_001")  | executeCount=0, successCount=0, failCount=0      |
| 2   | should toggle enabled task to disabled              | enableStatus="ENABLED"                       | 变为 "DISABLED"                                    |
| 3   | should toggle disabled task to enabled              | enableStatus="DISABLED"                      | 变为 "ENABLED"                                     |
| 4   | should return false when toggling non-existent task | id=999                                       | 返回 false                                         |
| 5   | should update execution stats for success           | status="SUCCESS", duration=200, newInsert=95 | executeCount+1, successCount+1, lastDuration=200 |
| 6   | should update execution stats for failure           | status="FAILED"                              | executeCount+1, failCount+1                      |
| 7   | should find enabled tasks                           | 无                                            | 返回状态为 ENABLED 的任务列表                              |

### 1.5 FieldMappingServiceTest（3 个用例）

| #   | 用例名称                                    | 输入                   | 预期结果                          |
| --- | --------------------------------------- | -------------------- | ----------------------------- |
| 1   | should find mappings by API config id   | apiConfigId=1        | 返回包含 sourceField="field1" 的列表 |
| 2   | should batch save field mappings        | apiConfigId=1, 2 条映射 | 先删除旧数据，再插入 2 条新数据             |
| 3   | should delete mappings by API config id | apiConfigId=1        | 返回 true                       |

### 1.6 DashboardServiceTest（2 个用例）

| #   | 用例名称                                          | 输入             | 预期结果                                                |
| --- | --------------------------------------------- | -------------- | --------------------------------------------------- |
| 1   | should compute dashboard stats with zero data | 全部返回空/0        | totalTasks=0, runningTasks=0, successRate=100       |
| 2   | should compute success rate correctly         | 成功 8 次, 失败 2 次 | successRate=80%, runningTasks=5, todayDataCount=500 |

### 1.7 TaskExecutorServiceTest（4 个用例）

| #   | 用例名称                                                    | 输入            | 预期结果                             |
| --- | ------------------------------------------------------- | ------------- | -------------------------------- |
| 1   | should throw exception when task not found              | taskId=999    | 抛出 RuntimeException              |
| 2   | should execute task successfully                        | taskId=1（存在）  | 创建日志 → 更新日志成功 → 更新任务统计           |
| 3   | should compute next execution time from cron expression | `0 */5 * * *` | 返回格式为 yyyy-MM-dd HH:mm:ss 的时间字符串 |
| 4   | should return time string even for invalid cron         | `"invalid"`   | 返回有效时间字符串（桩实现的行为）                |

### 1.8 ApiResponseTest（4 个用例）

| #   | 用例名称                                               | 输入                                    | 预期结果                                      |
| --- | -------------------------------------------------- | ------------------------------------- | ----------------------------------------- |
| 1   | should create success response with data           | ApiResponse.success("hello")          | code=200, message="Success", data="hello" |
| 2   | should create success response with custom message | ApiResponse.success("OK", 42)         | code=200, message="OK", data=42           |
| 3   | should create error response                       | ApiResponse.error("wrong")            | code=500, message="wrong", data=null      |
| 4   | should create error response with custom code      | ApiResponse.error(400, "Bad Request") | code=400                                  |

---

## 二、后端控制器层测试（20 个用例）

### 2.1 DashboardControllerTest（1 个用例）

| #   | 方法  | 路径                     | 预期                                         |
| --- | --- | ---------------------- | ------------------------------------------ |
| 1   | GET | `/api/dashboard/stats` | HTTP 200, `code=200`, `data.totalTasks=10` |

### 2.2 TokenConfigControllerTest（4 个用例）

| #   | 方法     | 路径                                   | 预期                                    |
| --- | ------ | ------------------------------------ | ------------------------------------- |
| 1   | GET    | `/api/token/list`                    | HTTP 200, `data[0].name="Test Token"` |
| 2   | GET    | `/api/token/detail/1`                | HTTP 200, `data.name="Detail Token"`  |
| 3   | POST   | `/api/token/save` (body=TokenConfig) | HTTP 200, `code=200`                  |
| 4   | DELETE | `/api/token/delete/1`                | HTTP 200, `code=200`                  |

### 2.3 ApiConfigControllerTest（4 个用例）

| #   | 方法     | 路径                                      | 预期                               |
| --- | ------ | --------------------------------------- | -------------------------------- |
| 1   | GET    | `/api/api-config/list`                  | HTTP 200, `data.records` 是数组     |
| 2   | GET    | `/api/api-config/detail/1`              | HTTP 200, `data.name="Test API"` |
| 3   | POST   | `/api/api-config/save` (body=ApiConfig) | HTTP 200, `code=200`             |
| 4   | DELETE | `/api/api-config/delete/1`              | HTTP 200, `code=200`             |

### 2.4 FieldMappingControllerTest（2 个用例）

| #   | 方法   | 路径                                      | 预期                                       |
| --- | ---- | --------------------------------------- | ---------------------------------------- |
| 1   | GET  | `/api/field-mapping/list/1`             | HTTP 200, `data[0].sourceField="field1"` |
| 2   | POST | `/api/field-mapping/save?apiConfigId=1` | HTTP 200, `code=200`                     |

### 2.5 TaskExecutionLogControllerTest（3 个用例）

| #   | 方法  | 路径                  | 预期                                           |
| --- | --- | ------------------- | -------------------------------------------- |
| 1   | GET | `/api/log/list`     | HTTP 200, `data.records` 是数组                 |
| 2   | GET | `/api/log/detail/1` | HTTP 200, `data.batchId="TASK_20260520_ABC"` |
| 3   | GET | `/api/log/recent`   | HTTP 200, `data` 是数组                         |

### 2.6 TaskSchedulerControllerTest（6 个用例）

| #   | 方法   | 路径                                              | 预期                                        |
| --- | ---- | ----------------------------------------------- | ----------------------------------------- |
| 1   | GET  | `/api/task/list`                                | HTTP 200, `code=200`                      |
| 2   | GET  | `/api/task/detail/1`                            | HTTP 200, `data.taskName="Test Task"`     |
| 3   | POST | `/api/task/save` (body=task)                    | HTTP 200, `code=200`                      |
| 4   | GET  | `/api/task/cron-description?cronExpression=...` | HTTP 200, `code=200`                      |
| 5   | POST | `/api/task/execute/1`                           | HTTP 200, `data="Task execution started"` |
| 6   | GET  | `/api/task/enabled`                             | HTTP 200, `code=200`                      |

---

## 三、前端测试（30 个用例）

### 3.1 API 模块测试 — index.test.js（18 个用例）

| #   | API 模块          | 用例                                                      |
| --- | --------------- | ------------------------------------------------------- |
| 1   | dashboardApi    | 验证 `getStats()` 调用 `GET /dashboard/stats`               |
| 2   | taskApi         | 验证 `getList()` 调用 `GET /task/list` 并传递分页参数              |
| 3   | taskApi         | 验证 `save()` 调用 `POST /task/save` 并传递请求体                 |
| 4   | taskApi         | 验证 `delete()` 调用 `DELETE /task/delete/{id}`             |
| 5   | taskApi         | 验证 `toggle()` 调用 `POST /task/toggle/{id}`               |
| 6   | taskApi         | 验证 `execute()` 调用 `POST /task/execute/{id}`             |
| 7   | taskApi         | 验证 `getCronDescription()` 传递 `cronExpression` 参数        |
| 8   | taskApi         | 验证 `getEnabledTasks()` 调用 `GET /task/enabled`           |
| 9   | tokenApi        | 验证 `getList()` 调用 `GET /token/list`                     |
| 10  | tokenApi        | 验证 `save()` 调用 `POST /token/save`                       |
| 11  | tokenApi        | 验证 `delete()` 调用 `DELETE /token/delete/{id}`            |
| 12  | apiConfigApi    | 验证 `getList()` 调用 `GET /api-config/list`                |
| 13  | apiConfigApi    | 验证 `save()` 调用 `POST /api-config/save`                  |
| 14  | apiConfigApi    | 验证 `delete()` 调用 `DELETE /api-config/delete/{id}`       |
| 15  | fieldMappingApi | 验证 `getList()` 调用 `GET /field-mapping/list/{id}`        |
| 16  | fieldMappingApi | 验证 `save()` 调用 `POST /field-mapping/save?apiConfigId=X` |
| 17  | logApi          | 验证 `getList()` 调用 `GET /log/list`                       |
| 18  | logApi          | 验证 `getDetail()` 调用 `GET /log/detail/{id}`              |

### 3.2 路由测试 — index.test.js（8 个用例）

| #   | 用例                                                                                          |
| --- | ------------------------------------------------------------------------------------------- |
| 1   | 所有 6 个路由名称已注册（Dashboard, TaskScheduler, TokenManagement, ApiManagement, FieldMapping, Logs） |
| 2   | 根路径 `/` 重定向到 `/dashboard`                                                                   |
| 3   | `/dashboard` 路径存在且组件为 Dashboard                                                             |
| 4   | `/task-scheduler` 路径存在                                                                      |
| 5   | `/token-management` 路径存在                                                                    |
| 6   | `/api-management` 路径存在                                                                      |
| 7   | `/field-mapping` 路径存在                                                                       |
| 8   | `/logs` 路径存在                                                                                |

### 3.3 App.vue 组件测试（2 个用例）

| #   | 用例           |
| --- | ------------ |
| 1   | 组件成功挂载       |
| 2   | 组件渲染 HTML 内容 |

### 3.4 Dashboard.vue 组件测试（2 个用例）

| #   | 用例                              |
| --- | ------------------------------- |
| 1   | 组件成功挂载，`.page-container` 存在     |
| 2   | 页面结构包含 3 个 `.dashboard-row` 行区域 |

---

## 四、页面功能测试清单（手动验证）

以下功能需要启动前后端后进行手动验证：

### 4.1 仪表盘页面

- [ ] 四个统计卡片正确显示：总任务数、运行中任务、今日成功率、今日数据量
- [ ] 每个卡片图标颜色正确（蓝/绿/橙/红）
- [ ] 饼图正常渲染（任务状态分布）
- [ ] 最近执行记录表格显示数据
- [ ] 今日执行统计区域显示成功/失败/新增数据
- [ ] 窗口缩放时饼图自适应

### 4.2 任务调度页面

- [ ] 任务列表分页展示
- [ ] 搜索功能（按任务名称/编码）
- [ ] 新建任务对话框：字段校验、API 配置下拉、Cron 验证
- [ ] 编辑任务
- [ ] 立即执行任务（带确认弹窗）
- [ ] 启用/禁用切换
- [ ] 删除任务（带确认弹窗）

### 4.3 Token 管理页面

- [ ] Token 列表展示
- [ ] 新建 FIXED 类型 Token
- [ ] 新建 DYNAMIC 类型 Token（显示额外字段）
- [ ] 编辑/删除 Token

### 4.4 接口管理页面

- [ ] API 配置列表分页 + 搜索
- [ ] 新建/编辑 API 配置（含 Token 选择器）
- [ ] 删除 API 配置

### 4.5 映射配置页面

- [ ] API 配置下拉选择
- [ ] 字段映射表格可编辑
- [ ] 添加/删除字段行
- [ ] 至少选择一个唯一键才能保存
- [ ] 批量保存映射

### 4.6 日志页面

- [ ] 日志列表分页展示
- [ ] 按任务筛选
- [ ] 按状态筛选（成功/失败/运行中）
- [ ] 日志详情弹窗（批次 ID、状态、计时、请求/响应报文）

---

## 五、测试运行方式

```bash
# 后端测试
cd backend
mvn test

# 前端测试
cd frontend
npm test

# 前端测试（监听模式）
cd frontend
npm run test:watch
```
