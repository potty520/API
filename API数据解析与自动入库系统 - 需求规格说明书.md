这是一份为您编写的《API数据解析与自动入库系统需求文档》。这份文档详细描述了一个带有Web管理界面的系统，它不仅能够自动获取API Token、拉取数据并解析入库，还支持任务的可视化管理。

---

# API数据解析与自动入库系统 - 需求规格说明书

| 文档版本 | 修改日期       | 修改人          | 修改说明   |
|:---- |:---------- |:------------ |:------ |
| V1.0 | 2026-05-20 | AI Assistant | 初始版本创建 |

## 1. 项目背景与目标

许多业务场景需要定时从第三方API获取数据（如天气、股票、企业内部数据）。手动操作不仅繁琐，而且容易遗漏Token过期、数据格式变更等问题。本项目旨在构建一个自动化系统，用户通过Web界面配置接口和解析规则后，系统自动完成“获取Token -> 调用业务接口 -> 解析JSON/XML -> 写入数据库”的全流程，并将结果可视化展示。

## 2. 用户角色

- **管理员：** 拥有最高权限，可配置数据源连接、接口地址、Token获取策略、管理所有任务。
- **普通用户：** 可查看任务执行状态、数据日志，但不能修改核心配置。

## 3. 功能性需求

### 3.1 模块一：Token 动态获取与管理

该模块负责管理所有API的认证凭据，确保业务接口调用时有合法的令牌。

- **Token获取策略配置：**
  
  - 支持固定Token：直接在输入框填写Token字符串。
  - 支持动态获取（OAuth 2.0 / 自定义登录）：
    - **获取接口配置：** 可配置Token获取的URL、请求方式（POST/GET）。
    - **参数体构造：** 支持表单参数（如 `grant_type`, `client_id`, `client_secret`）或自定义JSON Body。
    - **响应解析规则：** 支持通过JSONPath（如 `$.access_token`）或正则表达式，从接口返回的JSON中提取Token字段。
    - **过期处理：** 支持配置过期时间字段（JSONPath提取 `$.expires_in`），系统将提前刷新。若未提供，可手动设置有效时长（秒）。

- **Token刷新与生命周期：**
  
  - **自动刷新：** 在Token过期前N分钟（可配置，默认2分钟）自动调用获取接口刷新。
  - **失败重试：** 获取Token失败后，间隔1分钟重试3次，全部失败则暂停关联任务并推送告警。
  - **全局引用：** 业务接口在配置时，只需选择已配好的Token源，系统自动在请求头或参数中注入Token。

### 3.2 模块二：接口解析规则配置

该模块是核心，通过可视化方式定义如何拉取数据，并将半结构化的API响应转化为数据库表结构。

- **接口管理：**
  
  - 配置业务API URL、请求方式、参数（支持静态参数和占位符，如 `{date}`）。
  - 关联Token源（选择模块一配置的凭据）。

- **数据解析定义：**
  
  - **数据路径：** 使用JSONPath（如 `$.data.list`）指定需要入库的数组数据所在位置。
  - **字段映射（解析器）：**
    - 可视化表格建立“源字段 -> 目标数据库字段”的映射。
    - 源字段支持JSONPath。
    - 支持字段脱敏配置（如手机号中间4位星号处理）。
    - 支持基础数据类型转换（字符串转数字、时间戳转日期）。

- **入库策略：**
  
  - **全量替换：** 清除目标表旧数据，写入新数据。
  - **增量追加：** 只插入新数据。
  - **存在更新（Upsert）：** 选择唯一键（如ID），存在则更新，不存在则插入。系统自动生成 `MERGE` 或 `INSERT ON DUPLICATE KEY UPDATE` 逻辑。

### 3.3 模块三：定时任务调度与监控

- **任务组装：** 将“Token获取”和“数据解析入库”串联成一个完整的任务流（DAG图：Token前置 -> 数据拉取 -> 解析清洗 -> 入库）。
- **调度配置：** 支持Cron表达式，精确控制执行周期（如每5分钟、每天8点）。
- **状态监控：** 实时显示最近一次运行时长、数据量、状态（成功/失败/运行中）。

### 3.4 模块四：数据预览与日志

- **数据预览：** 在Web页面上直接预览入库后的前50条数据，以表格形式展示。
- **执行日志：**
  - 详细记录每次任务执行的“请求报文（敏感信息脱敏）”和“响应报文”。
  - 记录错误堆栈信息，方便排查接口字段变更。

## 4. Web 页面设计（功能列表）

系统采用左侧菜单布局，需包含以下页面：

| 页面名称         | 主要功能组件                                   |
|:------------ |:---------------------------------------- |
| **仪表盘**      | 任务执行成功率饼图、今日拉取数据总量、最近失败任务列表。             |
| **Token 管理** | Token源列表（增删改查）、获取测试按钮、状态标签（有效/即将过期/已过期）。 |
| **接口管理**     | 接口列表、新增/编辑表单（含YAML/JSON配置导入）、手动单次执行按钮。   |
| **映射配置**     | 拖拽式字段映射表格、入库策略选择、SQL预览窗口。                |
| **任务调度**     | 任务流状态展示、Cron表达式输入器、任务启停开关、手动触发按钮。        |
| **数据与日志**    | 数据源/表选择器、数据表格预览、错误日志搜索（按时间/关键词）。         |

**核心交互细节：**

1. **Token连接测试：** 在Token配置页点击测试，系统用填写的账号密码请求一次Token，并返回结果，提示“连接成功，Token为: eyJhbG...”。
2. **数据解析调试：** 在接口管理页，可以点击“拉取并预览”，系统实时请求一次业务接口，不执行入库，而是在弹窗中显示解析后的JSON表格，验证字段映射是否正确。
3. **Cron辅助生成：** 调度配置旁提供常用Cron示例（如每分钟、每小时、每天）。

## 5. 技术栈建议（非功能需求）

- **后端框架：** Python (Flask/FastAPI) 或 Java (Spring Boot)。推荐 **Python (FastAPI)**，因其对异步IO和JSON处理支持极佳。
- **任务调度：** APScheduler (轻量级Python方案) 或集成 Celery + Redis (大规模方案)。
- **数据库：**
  - **元数据库：** MySQL/PostgreSQL（存储配置、日志）。
  - **目标业务库：** 需支持动态创建连接，兼容MySQL、PostgreSQL、SQL Server。
- **前端框架：** Vue.js 或 React，搭配 UI 库 (Ant Design / Element Plus)。
- **关键依赖库：** `jsonpath_ng` (数据提取), `httpx` (异步请求), `croniter` (Cron解析)。

## 6. 验收标准

1. 能成功配置一个带有Token过期机制的接口（如获取微信AccessToken），系统能在Token过期后自动重获，不影响后续数据拉取。
2. 能将复杂嵌套JSON（含多层数组）正确解析并存入指定的数据库表中，入库数据条数与JSON中对象数量一致。
3. Web界面操作流畅，配置一个新的接口从开始到入库成功，在熟练操作下用时不超过3分钟。
   
   
   
   ### 核心调整说明
   1. **自动建表机制**：系统将接管目标表的创建过程，用户不再需要手动在数据库中建表。只要配置了字段映射，系统在执行任务时会自动分析数据结构并生成 `CREATE TABLE` 语句。
   2. **严格的增量插入**：放弃全量替换和Upsert等复杂策略，统一采用**追加模式**，且必须根据“业务唯一键”去重，保证已有数据不会被修改或重复插入。
   
   ---
   
   ### 新增/修改的功能模块详情
   
   #### 模块二：接口解析与“零代码”建表 (重点修改)
   
   本模块在原有基础上增加了动态建表和增量控制功能。
   
   - **1. 智能字段映射与表结构定义**
     
     - **字段映射表（UI核心）**：在Web界面配置时，用户需填写一个表格，定义源字段（JSONPath）与目标表字段的对应关系。
     - **类型自动推断**：系统拉取到第一份样本数据后，会自动推断数据类型。
       - JSON的 `"abc"` → 自动映射为 `VARCHAR(255)`
       - `123` → 自动映射为 `INT` 或 `BIGINT`
       - `true/false` → 自动映射为 `TINYINT(1)`
       - 嵌套对象 → 自动映射为 `TEXT` 或 `JSON` 类型（取决于数据库方言）
     - **手动修正类型**：用户可点击字段旁的设置图标，手动修改数据库类型（如将VARCHAR长度从255调整为500，或指定为 `DECIMAL`）。
     - **指定业务唯一键（重要）**：用户必须在映射列表中勾选一个或多个字段作为**业务唯一键**，这是去重入库的依据。
   
   - **2. 自动建表策略**
     
     - **执行时机**：任务首次执行时（或目标表不存在时）。
     - **命名规则**：
       - 表名默认等于“接口任务名称”，或由用户在配置中自定义。
       - 系统会自动添加两个系统字段用于审计：
         - `_sys_insert_time`：记录该行首次进入系统的时间，`DEFAULT CURRENT_TIMESTAMP`。
         - `_sys_batch_id`：记录该行是由哪次任务执行写入的（格式：`TASK_20260520_001`）。
     - **SQL生成与执行**：
       - 系统根据字段映射和类型，拼装 `CREATE TABLE IF NOT EXISTS` 语句。
       - **容错机制**：如果数据库已存在同名表但结构不同（如缺少某列），系统会尝试执行 `ALTER TABLE ADD COLUMN` 自动补齐，而不是删表重建。
     - **索引创建**：系统会自动在用户指定的“业务唯一键”上创建唯一索引，以提高后续插入时的去重查询速度。
   
   #### 模块三：数据入库逻辑 (修改为“仅追加不重复”)
   
   - **入库策略（统一标准）**：
     - **唯一策略**：**只插入新数据（增量追加并去重）**。
     - **处理流程（伪逻辑）**：
       
       ```
       1. 接收解析好的数据列表 List<Map>  newData
       2. 从 newData 中提取所有的“业务唯一键”值
       3. 在数据库目标表中，批量查询这些键值是否已存在（只查唯一键列）
       4. 对比过滤，得到真正需要插入的新记录列表 insertList
       5. 如果 insertList 不为空，执行批量 INSERT 操作
       6. 记录日志：“总解析 100 条，已存在 98 条，本次新增插入 2 条”
       ```
     - **数据不变性**：对于已入库的业务数据，其值一旦写入，系统不会通过本任务进行任何更新操作。
   
   ---
   
   ### Web 页面交互补充
   
   **在“映射配置”页面增加以下元素：**
   
   1. **目标表定义区**：
      
      - **表名输入框**：支持输入动态变量（如 `ods_order_{date}`）或固定表名。
      - **唯一键选择器**：在映射字段列表每行末尾有一个复选框，要求用户必须勾选至少一个字段作为“业务唯一键”。勾选后，该字段旁会出现钥匙图标。
      - **建表语句预览按钮**：点击后会弹窗显示系统即将执行的完整 `CREATE TABLE` SQL 语句，供 DBA 或高级用户审核。
   
   2. **首次执行引导**：
      
      - 点击“保存并启动”时，若目标表不存在，系统弹窗提示：“目标表 `xxx` 不存在，系统将在首次执行时自动创建。唯一键为：`[order_id, sku_id]`。”
   
   ---
   
   ### 更新后的部分验收标准
   
   - **自动建表验收**：创建一个全新任务，指向一个空的数据库。配置好映射规则并启动任务。验收点：数据库里自动生成了结构准确的目标表，字段类型符合映射定义，且包含 `_sys_insert_time` 和 `_sys_batch_id` 两个审计字段。
   - **增量去重验收**：首次执行后，目标表有100条数据。在不修改API源数据的情况下，第二次执行该任务。验收点：任务日志显示“新增插入 0 条”，目标表数据仍为100条，没有产生重复记录。
   - **结构自适应验收**：在映射配置中增加一个新字段。下次任务执行时，系统能自动检测到该列不存在，并在日志中记录 `ALTER TABLE ADD COLUMN new_field VARCHAR(255)` 的执行情况，且不影响原有数据。
   
   
